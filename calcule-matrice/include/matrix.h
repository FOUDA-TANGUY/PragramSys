#ifndef __MATRIX__H__
#define __MATRIX__H__

    matrice transposition(matrice*);
    matrice addition(matrice*, matrice*);
    matrice multiplicationScalaire(matrice*, float);
    matrice multiplication(matrice*,matrice*);
    

#endif